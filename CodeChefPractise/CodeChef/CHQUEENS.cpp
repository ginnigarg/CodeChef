#include <iostream>
using namespace std;

int main() {
    int tc;
    cin >> tc;
    while(tc--) {
        int n, m, x, y;
        cin >> n >> m >> x >> y;
        int** arr = new int*[n +  1];
        for(int i = 1; i <= x; i++) {
            *(arr + i) = new int[y + 1];
        }
        




    }








}
