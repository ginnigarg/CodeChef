#include <iostream>
#include <algorithm>
using namespace std;

int main() {
    int tc;
    cin >> tc;
    while(tc--) {
        int cards, turns;
        cin >> cards >> turns;
        int* values = new int[cards];
        int* number = new int[cards];
        int* turn = new int[turn];
        for(int i = 0; i < cards; i++) {
            cin >> values[i];
        }
        for(int i = 0; i < cards; i++) {
            cin >> number[i];
        }
        for()
    }

}
